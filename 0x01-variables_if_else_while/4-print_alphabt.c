#include <stdio.h>
/**
 * main. Program to print alphabet in lowercase
 * followed by a new line
 * Return: 0(Successful)
 */

int main(void)
{
char ch;
 for (ch = 'a' ; ch = 'z' ; ch++)
   {    (ch != 'e' && ch!= 'q')
